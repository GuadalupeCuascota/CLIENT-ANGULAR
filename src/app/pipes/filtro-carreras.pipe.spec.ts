import { FiltroCarrerasPipe } from './filtro-carreras.pipe';

describe('FiltroCarrerasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCarrerasPipe();
    expect(pipe).toBeTruthy();
  });
});
