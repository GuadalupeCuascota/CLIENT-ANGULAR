import { FiltroNoticiasPipe } from './filtro-noticias.pipe';

describe('FiltroNoticiasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroNoticiasPipe();
    expect(pipe).toBeTruthy();
  });
});
